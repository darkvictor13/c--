#include uma lib aleatoria

int main () {
    // comentario
    int vet[5];
    char caractere = 'a';
    const char *string = "ola mundo";
    char enter = '\n';
    float pi = 3.14;
    int numero = 10;
    if (numero >= 10) {
        numero += pi;
        numero -= vet[3];
    }else {
        numero /= (10 + 2 - 1 * sizeof(int));
    }
}
